# In memory database
# CONNECTION_STRING = "sqlite:///:memory:"

# MySQL database for use with docker-compose
# CONNECTION_STRING = "mysql+pymysql://root:dockerroot@mariadb/test"

# Test database for travis and coverage purposes.
CONNECTION_STRING = "sqlite:///testdatabase.db"